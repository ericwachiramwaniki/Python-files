# Define a list of numbers
numbers = [10, 23, 34, 47, 59]

# Use the built-in sum() function to calculate the sum
total = sum(numbers)

# Print the sum
print("The sum of all items in the list is:", total)
